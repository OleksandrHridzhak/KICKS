enum Gender {
  MALE
  FEMALE
  OTHER
}

model User {
  id       String @id @default(uuid())
  email    String @unique
  password String

  gender    Gender?
  firstName String?
  lastName  String?

  createdAt DateTime @default(now())
}
