info:
  name: Products catalog
  type: http
  seq: 3

http:
  method: GET
  url: "{{url}}/products/catalog"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
